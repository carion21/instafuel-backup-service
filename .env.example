# === Connexion PostgreSQL (utilisateur read-only dédié) ===
PGHOST=viaduct.proxy.rlwy.net
PGPORT=48229
PGDATABASE=railway
PGUSER=instafuel_backup
PGPASSWORD=***remplacer-par-mot-de-passe-readonly***

# === MinIO (accès limité au bucket instafuel/backups/) ===
MINIO_ENDPOINT=minio-api.beta.geasscorp.com
MINIO_ACCESS_KEY=***remplacer-apres-setup-minio-access***
MINIO_SECRET_KEY=***remplacer-apres-setup-minio-access***
MINIO_BUCKET=instafuel

# === Environnement ===
NODE_ENV=production

# === Cron Railway (1 seul service, auto-détection daily/hourly) ===
RAILWAY_CRON_SCHEDULE=5 * * * *
